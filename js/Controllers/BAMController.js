angular.module('BAMApp.controllers').controller('BAMController', function ($scope) {
    $scope.models = {
        Message: 'This section will be implemented in sprint 3. It will contain the running totals for the credits etc.'
    }
});
